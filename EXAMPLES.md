# Примеры использования детектора аномалий

## 1. Первый запуск (инициализация)

```bash
# Загрузить данные за последние 60 торговых дней
python detector.py --init --days 60
```

**Что произойдет:**
- Скрипт создаст директории `data/` и `reports/`
- Загрузит данные с API Мосбиржи за последние 60 торговых дней
- Сохранит каждый день в отдельный JSON файл
- Займет ~2-3 минуты (с учетом пагинации и пауз)

**Пример вывода:**
```
======================================================================
ИНИЦИАЛИЗАЦИЯ: Загрузка данных за последние 60 торговых дней
======================================================================

Период: 2025-11-15 - 2026-02-02

[1/60] 2025-11-15
  Загрузка данных с API за 2025-11-15... OK (287 записей)
[2/60] 2025-11-18
  Загрузка данных с API за 2025-11-18... OK (289 записей)
...
```

---

## 2. Анализ конкретной даты

```bash
# Проанализировать аномалии за 31 января 2026
python detector.py --date 2026-01-31
```

**Что произойдет:**
1. Определит базовый период: 24-30 января (5 торговых дней)
2. Загрузит данные за эти дни (из кеша или API)
3. Рассчитает статистику по каждому тикеру
4. Найдет аномалии (Z-score > 2.0)
5. Создаст отчеты в `reports/`

**Пример вывода:**
```
======================================================================
АНАЛИЗ АНОМАЛИЙ ОБЪЕМОВ
======================================================================

Целевая дата: 2026-01-31
Базовый период: 2026-01-24 - 2026-01-30 (5 дней)
Порог аномалии: 2.0σ

Загрузка данных за базовый период:
  Загрузка из кеша: 2026-01-24
  Загрузка из кеша: 2026-01-27
  Загрузка из кеша: 2026-01-28
  Загрузка из кеша: 2026-01-29
  Загрузка из кеша: 2026-01-30

Загрузка данных за целевую дату:
  Загрузка из кеша: 2026-01-31

✓ Загружено: 287 тикеров за 2026-01-31

Расчет статистики...
✓ Проанализировано: 287 тикеров
✓ Найдено аномалий: 12

✓ Текстовый отчет: reports/anomalies_2026-01-31.txt
✓ JSON отчет: reports/anomalies_2026-01-31.json

======================================================================
АНОМАЛИИ ОБЪЕМОВ ТОРГОВ
...
```

---

## 3. Ежедневное использование

```bash
# Анализировать вчерашний день (по умолчанию)
python detector.py
```

**Когда запускать:** Каждое утро после 03:00 МСК (когда данные за вчера появятся в API)

**Пример:** Сегодня 03.02.2026, запускаем скрипт без параметров
- Автоматически возьмет вчерашний день: 02.02.2026
- Загрузит данные если их еще нет
- Построит отчет

---

## 4. Принудительное обновление данных

```bash
# Если нужно перезапросить данные с API (игнорируя кеш)
python detector.py --date 2026-01-31 --force
```

**Когда использовать:**
- Подозреваете, что данные в кеше устарели
- API вернул неполные данные при первой загрузке
- Хотите убедиться в актуальности данных

---

## 5. Бэктестинг на исторических датах

```bash
# Протестировать детектор на прошлых датах
python detector.py --date 2026-01-15
python detector.py --date 2026-01-20
python detector.py --date 2026-01-25
python detector.py --date 2026-01-31
```

**Цель:** Проверить как детектор находил бы аномалии в прошлом

---

## 6. Интерпретация результатов

### Пример отчета

```
[1] SBER - Сбербанк
    Оборот: 4.5 млрд руб          ← Оборот в целевой день
    Средний: 2.9 млрд руб         ← Средний оборот за 5 дней до
    Z-score: +3.67                ← Сила аномалии (3.67σ)
    Отклонение: +57.9%            ← На 57.9% выше среднего
```

**Интерпретация:**
- **Z-score +3.67** - экстремальная аномалия (случается в 0.01% случаев)
- **Отклонение +57.9%** - объем на 58% выше обычного
- **Возможные причины:** важная новость, дивиденды, M&A, санкции

### Уровни тревожности

| Z-score | Уровень | Вероятность | Действие |
|---------|---------|-------------|----------|
| 2.0-2.5 | Умеренная аномалия | ~2-5% | Обратить внимание |
| 2.5-3.0 | Сильная аномалия | ~0.3-2% | Проверить новости |
| 3.0+ | Экстремальная | <0.3% | Срочно изучить причину |

---

## 7. Автоматизация (Linux/macOS)

### Создать cron задачу

```bash
# Открыть редактор cron
crontab -e
```

### Добавить задачу (запуск каждое утро в 9:00)

```bash
# Анализ аномалий объемов (пн-пт в 9:00)
0 9 * * 1-5 cd /home/user/moex_detector && /usr/bin/python3 detector.py >> /tmp/detector.log 2>&1
```

---

## 8. Настройка порога аномалии

### Изменить порог в коде

Откройте `detector.py` и измените значение:

```python
# Было: 2.0σ (баланс точности и чувствительности)
ANOMALY_THRESHOLD_SIGMA = 2.0

# Стало: 2.5σ (меньше сигналов, выше надежность)
ANOMALY_THRESHOLD_SIGMA = 2.5

# Или: 1.5σ (больше сигналов, ниже надежность)
ANOMALY_THRESHOLD_SIGMA = 1.5
```

**Рекомендации:**
- **2.0σ** - универсальный порог (по умолчанию)
- **2.5σ** - если слишком много ложных срабатываний
- **3.0σ** - только экстремальные случаи
- **1.5σ** - для агрессивного отслеживания

---

## 9. Работа с отчетами

### Текстовый отчет (для людей)

```bash
# Посмотреть отчет
cat reports/anomalies_2026-01-31.txt

# Найти конкретный тикер
grep "SBER" reports/anomalies_2026-01-31.txt
```

### JSON отчет (для автоматизации)

```python
import json

# Загрузить отчет
with open('reports/anomalies_2026-01-31.json') as f:
    report = json.load(f)

# Получить топ-3 аномалии
top3 = report['anomalies'][:3]

# Отправить уведомление
for anomaly in top3:
    send_telegram_message(
        f"🚨 Аномалия: {anomaly['ticker']} "
        f"(Z={anomaly['z_score']})"
    )
```

---

## 10. Типичные сценарии использования

### Сценарий 1: Утренний ритуал трейдера

```bash
# 1. Запустить детектор
python detector.py

# 2. Открыть отчет
cat reports/anomalies_$(date -d "yesterday" +%Y-%m-%d).txt

# 3. Изучить топ-5 аномалий
# 4. Проверить новости по этим компаниям
# 5. Принять торговое решение
```

### Сценарий 2: Еженедельный обзор

```bash
# Проанализировать все дни недели
for day in {25..29}; do
    python detector.py --date 2026-01-$day
done

# Собрать все отчеты
cat reports/anomalies_2026-01-*.txt > weekly_review.txt
```

### Сценарий 3: Исследование аномалии

```bash
# Нашли аномалию по SBER 31 января
# Проверим предыдущие дни

python detector.py --date 2026-01-28
python detector.py --date 2026-01-29
python detector.py --date 2026-01-30
python detector.py --date 2026-01-31

# Смотрим динамику аномалий по SBER
```

---

## Частые вопросы

**Q: Почему вчерашний день, а не сегодняшний?**  
A: Данные за текущий день появляются в API только на следующее утро (~03:00 МСК)

**Q: Можно ли анализировать выходные дни?**  
A: Нет, биржа не работает в выходные. Скрипт выдаст предупреждение "Нет данных за дату"

**Q: Что значит "недостаточно данных"?**  
A: Для новых компаний может быть меньше 5 дней истории. Скрипт использует что есть и выдает warning

**Q: Как часто обновлять данные?**  
A: По умолчанию скрипт использует кеш. Обновление нужно только если подозреваете устаревшие данные

**Q: Можно ли анализировать не только акции?**  
A: Сейчас скрипт настроен только на акции. Для облигаций/фьючерсов нужны изменения в API запросе

---

Готово! Теперь можно начинать использовать детектор 🚀
